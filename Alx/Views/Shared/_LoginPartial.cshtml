@if (Request.IsAuthenticated) { 
    <span style="line-height:50px; vertical-align:middle; padding-right:20px;" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"></i>
        <span style="height:auto; font-size:larger; margin-top:10px; color:whitesmoke;">Buna, <b><i> @User.Identity.Name </i></b> </span>
        <b class="caret" style="color:whitesmoke;"></b>
    </span>
    <ul class="dropdown-menu">
        <!--<li>
            <a href="#"><i class="fa fa-fw fa-user"></i> Profile</a>
        </li>-->
        <li>
            <a href="@Url.Action("Index", "Home")"><i class="fa fa-fw fa-envelope"></i> Inbox</a>
        </li>
        <li>
            <a href="@Url.Action("Manage", "Account")"><i class="fa fa-fw fa-gear"></i> Settings</a>
        </li>
        <li class="divider"></li>
        <li>   
                @*@using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
                {
                    @Html.AntiForgeryToken()
                    <a href="javascript:document.getElementById('logoutForm').submit()">
                        <i class="fa fa-fw fa-power-off"></i> 
                        Log out
                    </a>
                }*@
            <a href="@Url.Action("LogOff", "Account")">
                <i class="fa fa-fw fa-power-off"></i>
                Log out
            </a>
        </li>
    </ul>
}else{
<a href="@Url.Action("Login", "Account" )" class="dropdown-toggle" data-toggle="dropdown">
    <i class="fa fa-user"></i>
    Login
    <b class="caret"></b>
</a>
<ul class="dropdown-menu">
    <li>
        <a href="@Url.Action("Register", "Account")"><i class="fa fa-fw fa-user"></i> Register</a>
    </li>
    <li>
        <a href="@Url.Action("Login", "Account")"><i class="fa fa-fw fa-envelope"></i> Login</a>
    </li>
    
</ul>
}